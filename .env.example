# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=https://localhost:3000,https://example.codespaces.preview.app

# Service account credentials for Firebase Admin.
# Option 1: base64-encoded JSON
FIREBASE_SERVICE_ACCOUNT=
# Option 2: path to a JSON file inside the repo or mounted secret
FIREBASE_SERVICE_ACCOUNT_PATH=

# Port the dev server should listen on
PORT=3000
